````markdown
## Install

```bash
python -m pip install -r requirements.txt --build-constraint build-constraints.txt
````

## Prepare the model

```bash
python download.py
```

Then copy the `*.safetensors` files to `mllm/`.

```
```
